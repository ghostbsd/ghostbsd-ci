pipeline {
  agent { label 'BUILDER2' }
  stages {
    stage('start test ') {
      steps {
        sh 'pytest -v --junitxml results/essential_packages.xml tests/essential_packages.py'
      }
      post {
        always {
          junit 'results/essential_packages.xml'
        }
      }
    }
  }
}